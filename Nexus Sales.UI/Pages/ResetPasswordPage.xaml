<Page x:Class="NexusSales.UI.Pages.ResetPasswordPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:NexusSales.UI.Pages"
      mc:Ignorable="d" 
      xmlns:Icons="http://metro.mahapps.com/winfx/xaml/iconpacks"
      xmlns:uc="clr-namespace:NexusSales.UI.UserControls"
      Title="ResetPasswordPage">

    <Page.Resources>
        <Style x:Key="TransparentIconButton" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Page.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock x:Name="StatusText" Grid.Row="0" HorizontalAlignment="Center" Foreground="LightBlue" FontWeight="Bold" Visibility="Collapsed"/>

        <!-- Title -->
        <TextBlock Grid.Row="0"
                   Text="Reset Password"
                   FontSize="32"
                   FontWeight="Bold"
                   FontFamily="Consolas"
                   Foreground="{StaticResource FontNormalBrush}"
                   HorizontalAlignment="Center"
                   Margin="32,24,32,24"/>

        <!-- Main Content -->
        <StackPanel Grid.Row="1" HorizontalAlignment="Center" VerticalAlignment="Center">
            <!-- New Password Input Styled -->
            <Border CornerRadius="20"
                    Background="{StaticResource BackgroundBrush}"
                    BorderBrush="{StaticResource FontNormalBrush}"
                    BorderThickness="1"
                    Width="220" Height="43"
                    HorizontalAlignment="Center" Margin="0,0,0,8">
                <Grid>
                    <!-- Hidden PasswordBox -->
                    <PasswordBox x:Name="NewPasswordBox"
                                 Background="Transparent"
                                 BorderThickness="0"
                                 Foreground="{StaticResource FontDarkBrush}"
                                 FontFamily="Consolas"
                                 FontWeight="Bold"
                                 FontSize="20"
                                 VerticalContentAlignment="Center"
                                 HorizontalContentAlignment="Left"
                                 Width="320"
                                 Height="43"
                                 Padding="14,0,0,0"
                                 PasswordChanged="NewPasswordBox_PasswordChanged"/>
                    <TextBox x:Name="NewPasswordTextBox"
                             Background="Transparent"
                             BorderThickness="0"
                             FontFamily="Consolas"
                             Foreground="{StaticResource FontDarkBrush}"
                             Style="{StaticResource LoginTextBoxStyle}"
                             
                             VerticalContentAlignment="Center"
                             HorizontalContentAlignment="Left"
                             
                             Padding="14,0,0,0"
                             Visibility="Collapsed"
                             TextChanged="NewPasswordTextBox_TextChanged"/>
                    <TextBlock x:Name="NewPasswordPlaceholder"
                               Text="New Password"
                               Foreground="{StaticResource FontDarkBrush}"
                               Margin="14,0,0,0"
                               VerticalAlignment="Center"
                               HorizontalAlignment="Left"
                               IsHitTestVisible="False"
                               Visibility="Visible"/>
                    <!-- Eye Icon Border -->
                    <Border Width="36" Height="36"
                            HorizontalAlignment="Right"
                            VerticalAlignment="Center"
                            Margin="0,0,4,0"
                            Background="Transparent"
                            Cursor="Hand"
                            MouseLeftButtonUp="NewPasswordEyeBorder_MouseLeftButtonUp">
                        <Icons:PackIconMaterial x:Name="NewPasswordIcon"
                                                Kind="EyeClosed"
                                                FontSize="22"
                                                Foreground="{StaticResource FontNormalBrush}"
                                                VerticalAlignment="Center"
                                                HorizontalAlignment="Center"/>
                    </Border>
                </Grid>
            </Border>

            <!-- Confirm Password Input Styled -->
            <Border CornerRadius="20"
                    Background="{StaticResource BackgroundBrush}"
                    BorderBrush="{StaticResource FontNormalBrush}"
                    BorderThickness="1"
                    Width="220" Height="43"
                    HorizontalAlignment="Center" Margin="0,0,0,8">
                <Grid>
                    <PasswordBox x:Name="ConfirmPasswordBox"
                                 Background="Transparent"
                                 BorderThickness="0"
                                 Foreground="{StaticResource FontDarkBrush}"
                                 FontFamily="Consolas"
                                 FontWeight="Bold"
                                 FontSize="20"
                                 VerticalContentAlignment="Center"
                                 HorizontalContentAlignment="Left"
                                 Width="320"
                                 Height="43"
                                 Padding="14,0,0,0"
                                 PasswordChanged="ConfirmPasswordBox_PasswordChanged"/>
                    <TextBox x:Name="ConfirmPasswordTextBox"
                             Background="Transparent"
                             BorderThickness="0"
                             Foreground="{StaticResource FontDarkBrush}"
                             Style="{StaticResource LoginTextBoxStyle}"
                             FontFamily="Consolas"
                            
                             VerticalContentAlignment="Center"
                             HorizontalContentAlignment="Left"
                            
                             Padding="14,0,0,0"
                             Visibility="Collapsed"
                             TextChanged="ConfirmPasswordTextBox_TextChanged"/>
                    <TextBlock x:Name="ConfirmPasswordPlaceholder"
                               Text="Confirm Password"
                               Foreground="{StaticResource FontDarkBrush}"
                               Margin="14,0,0,0"
                               VerticalAlignment="Center"
                               HorizontalAlignment="Left"
                               IsHitTestVisible="False"
                               Visibility="Visible"/>
                    <!-- Eye Icon Border -->
                    <Border Width="36" Height="36"
                            HorizontalAlignment="Right"
                            VerticalAlignment="Center"
                            Margin="0,0,4,0"
                            Background="Transparent"
                            Cursor="Hand"
                            MouseLeftButtonUp="ConfirmPasswordEyeBorder_MouseLeftButtonUp">
                        <Icons:PackIconMaterial x:Name="ConfirmPasswordIcon"
                                                Kind="EyeClosed"
                                                FontSize="22"
                                                Foreground="{StaticResource FontNormalBrush}"
                                                VerticalAlignment="Center"
                                                HorizontalAlignment="Center"/>
                    </Border>
                </Grid>
            </Border>

            <!-- Reset Password Button -->
            <uc:FirstButton Text="Reset Password" Height="48" Width="220" Margin="32,16,32,0"
                            GlowEffect="True" GlowColor="AliceBlue" ZoomEffect="True" ButtonColor="White"
                            Radius="20" BorderBrush="Transparent" BorderThickness="0.2"
                            Click="ResetPassword_Click" HorizontalAlignment="Center"/>
        </StackPanel>
        <Button Style="{StaticResource BackButtonStyle}"
        Content="&lt; Back"
        Click="BackButton_Click"
        Grid.Row="1"
        HorizontalAlignment="Left"
        VerticalAlignment="Bottom"
        Margin="24,0,0,24"
        Panel.ZIndex="100"/>
    </Grid>
</Page>
